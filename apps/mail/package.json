{
  "name": "@oficina-amarela/mail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "npm --prefix dashboard run build-only && wrangler deploy --dry-run",
    "deploy": "npm --prefix dashboard run build-only && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "email-explorer": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.131.1"
  }
}

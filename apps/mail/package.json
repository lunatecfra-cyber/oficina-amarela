{
  "name": "@oficina-amarela/mail",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "build": "cd dashboard && bun run build-only && cd .. && wrangler deploy --dry-run",
    "deploy": "cd dashboard && bun run build-only && cd .. && wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "email-explorer": "^1.1.0"
  },
  "devDependencies": {
    "typescript": "^7.0.2",
    "wrangler": "^4.131.1"
  }
}
